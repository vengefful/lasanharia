# Dependências
node_modules
.pnpm-store

# Build
dist
build
.next
.vite
*.tsbuildinfo

# Banco local (SQLite). NUNCA commitar — dados reais ficam só no servidor.
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Variáveis de ambiente (NUNCA commitar)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor/SO
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example

# Artefatos de Playwright local (caso volte a usar como devDep)
playwright-report
test-results
.playwright
ms-playwright
